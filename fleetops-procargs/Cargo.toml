[package]
name = "fleetops-procargs"
version = "0.1.0"
edition = "2021"
rust-version = "1.96"
description = "The one unsafe sysctl(KERN_PROCARGS2) call for fleetops, behind a safe fn"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
libc = "0.2"
